const request = require('supertest');
const knex = require('knex');

const server = require('../server');
const db = require('../data/db');

describe('reviews routes', () => {

  describe('get route', () => {

    it('should return list of reviews for a user', async () => {

      expect(true).toBe(true);

    });

  });

});
